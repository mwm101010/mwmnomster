<h1><%= @user.email %></h1>
<h3>Member Since: <%= @user.created_at.strftime("%B %d, %Y") %></h3>
<h4>Total number of coment is:<%=  @user.comments.count %></h4>
<h4>Total 5 star comments is:<%= @user.comments.where(rating: '5_stars').count %></h4>
